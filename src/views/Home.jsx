import React from 'react';
import Navigator from '../components/Navigator'

const Home = (props) => {

    return (
        <React.Fragment>
            <Navigator />
        </React.Fragment>
    );
}

export default Home;